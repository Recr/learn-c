#include <stdio.h>
#include <math.h>   //includes the math library

int main()
{

double a = pow(2, 5); // returns 2 raised to the power of 5
printf("%.0lf\n", a);

double b = sqrt(64); // returns the square root of 64
printf("%.0lf\n", b);

double c = round(1.55); // returns the nearest integer
printf("%.0lf\n", c);

}